{
  "name": "auth-app-root",
  "private": true,
  "scripts": {
    "build": "cd my-app && npm install && npm run build",
    "start": "cd my-app && npm install && npm run build && cd .. && npx serve my-app/dist -l tcp://0.0.0.0:${PORT:-10000}"
  }
}